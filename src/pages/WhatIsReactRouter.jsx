import React from "react";

const WhatIsReactRouter = () => {
  return (
    <div>
      <h2>¿Qué es React Router?</h2>
      <p>
        React Router es la biblioteca de enrutamiento estándar para aplicaciones
        React. Nos permite manejar la navegación entre diferentes vistas o
        componentes en nuestra aplicación. Además, mantiene la interfaz de
        usuario sincronizada con la URL actual, lo cual es esencial para
        aplicaciones de página única donde el contenido cambia dinámicamente sin
        recargar toda la página.
      </p>
    </div>
  );
};

export default WhatIsReactRouter;
